# YuBen backend — core web stack + contract validation + tests.
fastapi>=0.139.0
uvicorn[standard]>=0.51.0
pydantic>=2.13.4
python-dotenv>=1.2.2
jsonschema>=4.26.0
httpx>=0.28.1
pytest>=9.1.1

# Pipeline deps (B3 pipeline-wrapper, Phase 1 Wave 2):
#   google-api-python-client — YouTube Data API v3 (search / videos / channels) used
#     by the Gen-2 research scripts and the key-test probe (app/pipeline).
#   youtube-transcript-api    — transcript fetch for analyze_scripts (no API quota).
#   keyring                   — OS-keychain backend for the local YouTube-key store
#     (app/store/secrets.py; B1 deferred it here — file-store fallback still works).
google-api-python-client>=2.198.0
youtube-transcript-api>=1.2.4
keyring>=25.7.0

# Phase 4 direct adapter (DirectAnthropicAdapter): the official Anthropic SDK,
# used for the terminal-free Messages-API path (keyword expansion + narrative).
# The import is lazy, so the backend still starts if it's missing.
anthropic>=0.116.0

# OpenAI-compatible adapters (OpenAI, OpenRouter, Ollama) all speak the same
# chat-completions protocol, so one SDK covers all three. Lazily imported too.
openai>=2.9.0
