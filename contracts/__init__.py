"""YuBen contracts package — schemas, fixtures, and the Pydantic mirror.

Single source of truth: docs/CONTRACTS.md. The backend imports the Pydantic
models from `contracts.python.models` (with the repo root on sys.path).
"""
